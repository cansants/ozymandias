{% extends "base.twig" %}

{% block title %}Ozymandias{% endblock %}
{% block head %}
    {{ parent() }}
    <link rel="stylesheet" href="/css/album.css" />
    <style type="text/css">
        .important { color: #336699; }
        .cover{ position: relative; margin-bottom: 35px; }
        .cover.left kbd,
        .cover.right kbd{
          position: absolute;
          top:25%;
        }
        .cover.left kbd{
          right: 0;
        }
        .cover.right kbd{
          left: 0;
        }
    </style>
{% endblock %}
{% block content %}
<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">Album example</h1>
    <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
    <p>
      <a href="{{ path_for('comparador') }}" class="btn btn-primary">Recargar</a>
      <a href="{{ path_for('home') }}" class="btn btn-secondary">Volver</a>
    </p>
  </div>
</section>
<div class="album text-muted">
      <div class="container">
      <div class="row">
        <div class="col col-xs-6 text-center">
          <h3>Original</h3>
        </div>
        <div class="col col-xs-6 text-center">
          <h3>Guetzli</h3>
        </div>
      </div>

        {% for imagen in seleccion %}
          <div class="row">
            <div class="cover left col col-xs-4 text-center">
              <kbd>{{ datos[imagen].original.filesize }}</kbd>
              <img src="{{ path_for('cover', { 'imagen': imagen, 'calidad':'default' } ) }}" alt="">
              
              <!--<pre>{{ dump(datos[imagen].original.exif) }}</pre>-->
            </div>
            <div class="col col-xs-4 hidden-xs">

            </div>
            
            <div class="cover right col col-xs-4 text-center">
              <kbd>{{ datos[imagen].comprimida.filesize }}</kbd>
              <img src="{{ path_for('cover', { 'imagen': imagen, 'calidad':'default' } ) }}" alt="">
              
              <!--<pre>{{ dump(datos[imagen].comprimida.exif) }}</pre>-->
            </div>
          </div>
        {% endfor %}

      </div>
    </div>
    <footer class="text-muted">
      <div class="container">
        <p class="float-right">
          <a href="#">Back to top</a>
        </p>
        <p>Estas son algunas imágenes de muestra antes y despues de aplicar la compresion con <strong>guetzli</strong>.</p>
        <p>Necesitas ayuda? <a href="../../">Visit the homepage</a> or read our <a href="../../getting-started/">getting started guide</a>.</p>
      </div>
    </footer>
    <script src="/js/holder.min.js"></script>
{% endblock %}
